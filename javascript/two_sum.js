// Given an array of integers nums and an integer target, return indices of the two numbers such that they add up to target.
//
// You may assume that each input would have exactly one solution, and you may not use the same element twice.
//
// You can return the answer in any order.

var twoSum = function(numsArray, targetInteger) {
  // Create a new object where the keys are the original nums and the values are the difference between each number and the target integer
  // Create an empty object
  let numberIndex = {};
  let result = [];

  // Iterate over each number and its index in the numbers array
};
